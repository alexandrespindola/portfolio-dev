{
  "name": "portfolio-dev",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "check": "turbo run check",
    "deploy": "bun run build && wrangler pages deploy apps/web/dist --project-name=portfolio-dev --branch=main",
    "deploy:preview": "bun run build && wrangler pages deploy apps/web/dist --project-name=portfolio-dev --branch=preview",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.11.2",
    "wrangler": "^4.136.0"
  },
  "packageManager": "bun@1.3.12",
  "engines": {
    "node": ">=20"
  }
}
