# --- Cloudflare manual deploy (never commit the real token) ---
# Create a token in the IFV Cloudflare account with Pages:Edit permission,
# then set it locally before running `bun run deploy`.
CLOUDFLARE_ACCOUNT_ID=your_ifv_cloudflare_account_id
CLOUDFLARE_API_TOKEN=