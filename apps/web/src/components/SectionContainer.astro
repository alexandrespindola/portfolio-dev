---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = "", id } = Astro.props as Props;
---

<section
  id={id}
  data-section={id}
  class={`section scroll-m-20 mx-auto w-full max-w-6xl px-4 sm:px-6 ${className}`}
>
  <slot />
</section>
