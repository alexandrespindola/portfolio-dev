---
const PROJECTS = [
  {
    title: "Healthysthetics",
    description: "Creation of a website from scratch for Healthysthetics Micropigmentation and Aesthetic Clinic, in Murcia, Spain. With a modern and personalized layout, fast loading with CDN, appointment scheduling system and WhatsApp notification automation.",
    link: "https://healthysthetics.es",
    github: "#",
    image: "/projects/healthysthetics.webp",
    tags: ["WordPress", "Elementor", "Cal.com", "Bunny CDN"],
  },
];
---

{
  PROJECTS.map(({ title, description, tags, image }) => (
    <article>
      <h3>{title}</h3>
      <p>{description}</p>
      <ul>
        {tags.map((tag) => (
          <li>{tag}</li>
        ))}
      </ul>
      <img src={image} alt={`Project screenshot of ${title}`} />
    </article>
  ))
}
